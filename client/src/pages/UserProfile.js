import React, { Component } from "react";
import API from "../utils/API";

class Main extends Component {
    state = {};
    render() {
        return (
            
        );
    }
}

export default Main;